# EKS provisions the configured cloud load balancer through its installed
# service controller. Controller-specific annotations can be added as needed.
service:
  type: LoadBalancer
  externalTrafficPolicy: Cluster
